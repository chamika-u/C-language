// arrays in C
#include <stdio.h>
int main() {
    int age[5]; // declaring an array of integers to hold ages
    int marks[5] = {22, 23, 24, 25}; // declaring and assigning values to an array
    printf("marks of student 1: %d\n", marks[0]); // accessing first element of the array
    printf("marks of student 2: %d\n", marks[1]); // accessing second element of the array

    return 0;
}
